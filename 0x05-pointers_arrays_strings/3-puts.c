#include "main.h"
/**
 * _strlen - check the code
 * @str :pointer
 * Return: void.
 */
void _puts(char *str)
{
	int i = 0;

	while (*(s + i))
    {
        _putchar(*(s + i));
        i++;
    }
		_putchar('\n');

}

