#include "main.h"

/**
 * _isupper - check the code
 * @c : character is used
 * Return: Always 0.
 */
 int _isigit(int c)
 {
  if (c >= '0' && c <= '9')
  {
      return (1);
  }
  else
  {
      return (0);
  }
 }
